parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/Domain/'
            - '../src/Kernel.php'

    App\UI\Controller\:
        resource: '../src/UI/Controller/'
        tags: ['controller.service_arguments']

    _instanceof:
        App\App\CQRS\CommandHandlerInterface:
            tags:
                - { name: messenger.message_handler, bus: command.bus }
        App\App\CQRS\QueryHandlerInterface:
            tags:
                - { name: messenger.message_handler, bus: query.bus }